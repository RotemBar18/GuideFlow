# Screenshots

Drop PNGs here with these names so the README renders them:

Portal: `login.png`, `projects.png`, `flows.png`, `steps.png`, `step-editor.png`
SDK overlays (demo app): `tooltip.png`, `spotlight.png`, `modal.png`

Capture on a device/emulator: Android Studio **Running Devices** → camera icon, or `adb exec-out screencap -p > login.png`.
