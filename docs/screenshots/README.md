# Screenshots

Add PNGs here with these names so the main README renders them.

Portal: `login.png`, `projects.png`, `flows.png`, `steps.png`, `step-editor.png`
SDK overlays (demo app): `tooltip.png`, `spotlight.png`, `modal.png`

To capture, use Android Studio Running Devices (camera icon), or run:
`adb exec-out screencap -p > login.png`
